# The `su-collaborator` IDENTITY — identities-v1-2026-08-30 P-002 (D-007 / D-013 / D-017).
#
# ABSTRACT, like `base`: no workItem/spine, never loaded standalone — a layer in an
# `extends` stack. The `slots:` key is the identity discriminator (D-013): this
# document fills the EXCLUSIVE `collaboration-stance` slot (D-017: two stances toward
# one owner conflict rather than stack) — how the su works WITH the owner: address by
# name, the plan-is-a-proposal / WHO-route ask gate and its AUTO boundary, delivery
# discipline.
#
# Its text is `prompts/collaboration-stance.md` — a VERBATIM extraction of the stance
# tiles of `blueprints/base/prompts/su.md`, regenerated by `npm run gen:su-decomposition`
# and drift-checked by `su-decomposition.test.ts` — edit su.md, never the extraction.
# `prompts/<slot>.md` is the candidate naming for a non-domain slot document; P-003's
# renderer fixes the convention.
id: su-collaborator
extends: base
version: 0.1.0
description: >-
  IDENTITY (abstract — a layer, never runnable alone): the interactive su's
  collaborator stance toward the owner. Fills the exclusive `collaboration-stance`
  slot with su.md's Who-you-are address rule, Default-posture ask gate and Delivery
  discipline, extracted verbatim by P-002.

slots:
  - slot: collaboration-stance
