# The `papercusp-engineer` IDENTITY — identities-v1-2026-08-30 P-002 (D-007 / D-013 /
# D-017 / D-018).
#
# ABSTRACT, like `base`: it has no workItem/spine of its own and is never loaded
# standalone — it appears only as a layer in an `extends` stack. The `slots:` key
# is the identity discriminator (D-013): this document fills the EXCLUSIVE `domain`
# slot — the software-engineering PROFESSION: what the work IS and how it is judged.
#
# Its text is `prompts/agent-base-overlay.md` — the `domain` leaf name D-018 fixed
# (the file `resolveReplacementSystemPrompt` walks the extends chain for). It is a
# VERBATIM extraction of the engineering-discipline tiles of
# `blueprints/base/prompts/su.md` (Git · Engineering discipline · the papercusp-way
# routing gate), regenerated by `npm run gen:su-decomposition` and drift-checked by
# `su-decomposition.test.ts` — edit su.md, never the extraction.
#
# NOT YET ATTACHED to the coding hive: `blueprints/coding` still has no domain leaf
# (D-018 §5, EI-22285277325728666). The flip — `coding` extending this identity, and
# P-003's renderer producing the tier-appropriate rendering of it — is P-003 / P-023.
id: papercusp-engineer
extends: base
version: 0.1.2
visibility: public
description: >-
  IDENTITY (abstract — a layer, never runnable alone): the software-engineering
  profession. Fills the exclusive `domain` slot with su.md's engineering
  discipline, extracted verbatim by P-002; P-023 makes it the coding hive's
  domain document.

slots:
  - slot: domain
attestation:
  contentHash: c19c053c9891cf49534f5458fcf9302adcb271ccc14a2836f89ccfa64220a334
  signedBy: papercupai
